{
  "name": "kidrooproject-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "kidrooBackend"
  ],
  "scripts": {
    "start": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix kidrooBackend\"",
    "install-all": "npm install --prefix frontend && npm install --prefix kidrooBackend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
